# 星野 Xingye · 项目概况

> 更新时间：2026-09-05 · 版本 0.6.0 · 文档由当前代码库实际状态生成

---

## 一、项目定位

**星野 Xingye** 是一个 Windows 桌面级 QQ 智能助手应用：

- **桌面形态**：Tauri 2.0 壳（Rust + WebView2），主程序 `xingye.exe` 位于项目根目录，双击即用
- **能力**：QQ 群聊/私聊智能回复（AI 对话）、Steam 游戏查询与促销订阅、B 站视频解析、原神攻略查询、好友白名单管理、群广播、Web CLI 等
- **架构理念**：Tauri 壳负责窗口与子进程生命周期；Express 后端负责业务与面板托管；SnowLuma 作为内部 QQ 协议引擎（对用户零暴露）

---

## 二、架构与消息链路

```
┌─────────────────── 星野桌面 App (xingye.exe, Tauri 壳) ───────────────────┐
│  托盘(显示/重启服务/开机自启/退出) · 单实例锁 · 子进程生命周期管理          │
└──────────┬───────────────────────────────┬───────────────────────────────┘
           │ spawn + 管理                   │ spawn + 管理
           ▼                               ▼
┌─────────────────────┐          ┌──────────────────────────────┐
│ SnowLuma（内部引擎） │          │ bot-backend（业务后端）        │
│ QQ hook 注入引擎      │◀──WS────▶│ Express :3000                │
│ WebUI :5099(内部API)  │  :3001   │ · 面板静态托管 + REST API     │
│ OneBot WS 服务 :3001  │          │ · SnowLuma 代理层(/api/snowluma)│
└──────────┬──────────┘          │ · Dashboard WS 推送           │
           │ hook 注入            └──────────┬───────────────────┘
           ▼                                │ 面板(HTTP/WS)
        QQ.exe（多账号）                    ▼
                              管理面板（Vue 3，嵌入 exe）
```

**端口清单**：`:3000` 后端 API + 面板 · `:3001` OneBot WS · `:5099` SnowLuma 内部 API（不再需要人工访问）

---

## 三、目录结构（release 布局）

```
Xingye/
├── xingye.exe              # 桌面版主程序（构建后自动同步至此）
├── app/                    # 运行资源：bot-backend 编译产物 + 面板 dist + updater
│   ├── bot-backend/
│   ├── panel-frontend/dist/
│   └── version.json
├── SnowLuma/               # QQ 协议引擎（index.mjs + config/ + data/）
├── BBDown/                 # B 站下载工具（BBDown.exe）
├── version.json            # 版本号单一来源（API / 更新检查共用）
├── ecosystem.config.cjs    # PM2 配置（服务器部署用）
├── README.md
├── source/                 # 全部开发源码
│   ├── src-tauri/          #   Tauri 壳源码（Rust）
│   ├── panel-frontend/     #   面板前端源码（Vue 3 + Vite）
│   ├── bot-backend/        #   后端源码（TypeScript）
│   ├── scripts/            #   构建/发布脚本（build.js、release.js）
│   ├── updater/            #   服务器增量更新器
│   ├── tests/              #   单元测试
│   ├── docs/               #   历史文档（更新报告、部署指南）
│   └── server/             #   服务器分发（Dockerfile、部署脚本）
└── release/                # 发布产物输出（NSIS 安装包 / 增量包 / latest.json）
```

---

## 四、功能清单（面板标签页）

| 页面 | 功能 |
|---|---|
| 总览 | 服务状态、主机资源、AI/Token 统计、实时图表、最近告警 |
| 用量 | AI Token 消耗按日统计、请求数、月度汇总 |
| 功能管理 | Steam 订阅管理、游戏账号绑定、已知群组、**功能开关**（即时生效） |
| 系统设置 | 应用自更新（Tauri updater，签名校验）、群唤醒管理、更新广播、Prompt 在线修改、进程管理 |
| 日志 | 后端实时日志（SSE）、级别过滤、搜索、下载 |
| 消息 | 手动向群组发送消息 |
| 终端 | Web CLI（/status /say /restart 等） |
| 好友管理 | 好友白名单自动通过、申请记录处理 |
| **QQ 管理** | SnowLuma 全功能代理：QQ 进程注入/卸载/探测、账号头像与 OneBot 配置在线编辑（热重载）、实时日志流（SSE）、日志级别切换、SnowLuma 更新检查 |
| 连接 | 本地模式 / 远程服务器模式切换（桌面版可连远程星野服务器） |

**QQ 消息能力**：AI 对话（唤醒词「星野」/@）、Steam 战绩/时长/促销战报、B 站视频解析下载、原神面板与攻略图、赞我、多账号查询、群广播等。

---

## 五、启动 / 构建 / 发布

| 操作 | 方式 |
|---|---|
| 日常启动 | 双击根目录 `xingye.exe`（关闭=隐藏到托盘，托盘右键退出） |
| 构建桌面版 | `build-tauri.bat`（前端 → tauri build → 自动同步 exe 到根目录） |
| 开发调试 | `dev-tauri.bat` |
| 仅编译后端 | `npm run build:backend` |
| 仅构建面板 | `npm run build:frontend` |
| 发布新版 | `release.bat` → 服务器增量包 + NSIS 安装包 + latest.json 全部发到 GitHub Release |
| 服务器更新 | 服务器上 `node source/updater/server-update.js check/apply` |
| 运行测试 | `node --test source/tests/updater.test.js` |

---

## 六、版本与更新

- **当前版本**：0.6.0（`version.json` 为单一来源；桌面壳、面板、API 三处一致）
- **桌面自更新**：tauri-plugin-updater，端点指向 GitHub Release 的 `latest.json`，minisign 签名校验（私钥在 `~/.tauri/xingye.key`，务必备份）
- **发布历史**（2026-09-04/05 桌面化改造）：
  - `eebcae8` 初始快照 · `06552d6` 清理 Electron 旧壳 · `aa0ec63` 桌面级结构化
  - `a523576` NSIS 打包 · `7b9484d` 根目录主入口 · `597fd88` SnowLuma 全量集成
  - `92e5571` 代理白名单修复 · `109093d` 发布闭环 · `660c94a` 深色模式 · `afca687/597fd88` QQ 管理页
  - `7b9484d→source 归组` 目录 release 化（本轮）

---

## 七、主题与 UI

- **风格**：BakaXL 官网同款 Fluent 风格（主色 `rgb(40,147,240)`、浅色基底 `#F3F3F3`、胶囊按钮、Montserrat 字体）
- **双主题**：浅色 / 深色跟随系统（深色基底 `#202020` 为 BakaXL 官方暗色变量），全变量化驱动（`source/panel-frontend/src/App.vue` 的 `:root` 与 `prefers-color-scheme: dark` 分支）
- **质感**：acrylic 玻璃（backdrop-blur）+ 官网同款渐变背景光斑

---

## 八、已知事项与注意事项

1. **SnowLuma 绑定**：面板「QQ 管理」首次使用需输入一次 SnowLuma WebUI 密码（持久化后自动续期）
2. **编译锁定**：桌面版运行时 exe 被锁，重新编译前先退出（托盘右键退出）
3. **改前端必须重编译**：UI 是编译时嵌入 exe 的，改完 `vite build` 后需 `cargo build --release` 并同步 exe 到根目录（`build-tauri.bat` 一键完成）
4. **miao-data（145MB）**：原神元数据为下载型数据，`node source/bot-backend/genshin-guide/scripts/init-miao-data.ts` 重新拉取
5. **签名私钥**：`~/.tauri/xingye.key` 丢失将无法为老用户推送更新，请备份
6. **服务器部署**：走 `source/server/server-package.sh` 打包 → 上传 → `server-deploy.sh`，或 Docker（`source/server/`）
7. 全局代理 `127.0.0.1:8262` 存在时，本机 curl 需加 `--noproxy "*"`
